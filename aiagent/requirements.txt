fastapi
uvicorn[standard]
python-dotenv
aiohttp
pydantic
livekit            # realtime SDK + small helpers
livekit-api        # server SDK (room service, token helpers) # optional, for LLM/ TTS / STT integration
openai
gTTS               # optional fallback TTS
aiofiles
groq
websockets
httpx
python-multipart